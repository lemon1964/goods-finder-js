````md
# Goods Finder (Next.js App Router)

Учебное приложение-витрина: список товаров, поиск через URL, страница детали, избранное/история, мини-API внутри Next.

## 🌐 Продакшен (Render)

URL: https://XXXX-goods-finder-js.onrender.com/

## ⚙️ Стек

- Next.js (App Router)
- React
- Tailwind CSS
- Framer Motion
- Route Handlers (мини-бэкенд внутри Next)

## 🚀 Установка

```bash
git clone https://github.com/ВАШ_ЮЗЕРНЕЙМ/goods-finder-js.git
cd goods-finder-js
npm install
```

## 🔐 Переменные окружения

Создайте файл `.env.local` в корне проекта:

```bash
DUMMYJSON_API_BASE_URL=https://dummyjson.com
```

> `.env.local` не коммитится в репозиторий. На Render эта переменная задаётся во вкладке Environment.

## 🧪 Запуск в dev-режиме

```bash
npm run dev
```

Откройте [http://localhost:3000](http://localhost:3000)

## 🏗️ Проверка в “боевом” режиме (build/start)

```bash
npm run build
npm run start
```

Откройте [http://localhost:3000](http://localhost:3000)

## 🔗 Основные страницы

* `/` — главная
* `/goods` — список товаров + поиск
* `/goods/[id]` — деталь товара
* `/favorites` — избранное (localStorage)
* `/history` — история (localStorage)

## 🧩 Замечания

* Если в продакшене падает `/goods` и в логах видно `Missing env "DUMMYJSON_API_BASE_URL"`, значит переменная окружения не задана на Render.
* Внешние картинки берутся с `cdn.dummyjson.com`, поэтому источник должен быть разрешён в `next.config.mjs` (настройка `images.remotePatterns`).

````