// src/app/(app)/goods/layout.js
export const metadata = {
  title: "Товары",
  description: "Каталог товаров: список и карточка товара",
};

export default function GoodsLayout({ children }) {
  return <>{children}</>;
}
