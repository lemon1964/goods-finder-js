// src/app/(app)/layout.js
/*
  Группа маршрутов (app) — техника маршрутизации.
  Название группы не показывается в интерфейсе.
*/
export default function AppGroupLayout({ children }) {
  return <>{children}</>;
}
