// src/app/(site)/layout.js
/*
  Группа маршрутов (site) — техника маршрутизации.
  Название группы не показывается в интерфейсе.
*/
export default function SiteLayout({ children }) {
  return <>{children}</>;
}
